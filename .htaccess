# URLs propres — structure en répertoires
Options +FollowSymLinks -MultiViews
DirectorySlash Off
RewriteEngine On

# Rediriger /page.html → /page
RewriteRule ^(.+)\.html$ /$1 [R=301,L,NC]

# Rediriger /page/ → /page
RewriteRule ^([^/]+)/$ /$1 [R=301,L]
